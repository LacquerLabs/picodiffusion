# Python dependencies for picoDiffusion.
#
# PyTorch is NOT listed here — it is installed separately in the Dockerfile
# from the official PyTorch CUDA index to ensure GPU support.  If it were
# listed here, pip might install a CPU-only version and overwrite the
# CUDA build.

# Web framework and server
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
jinja2>=3.1.4

# Stable Diffusion inference
# Pinned to known-working versions for SD 1.5 from_single_file loading.
# Loose >= pins caused silent updates that broke CLIPTextModel loading in
# newer diffusers/transformers combinations.
diffusers==0.30.3
transformers==4.44.2,<5.6
accelerate==0.33.0
peft==0.12.0            # required for LoRA support (load_lora_weights)
safetensors>=0.4

# Image handling
Pillow>=10.0
